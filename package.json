{
  "name": "@nmb/personal-website",
  "private": true,
  "packageManager": "yarn@3.2.2",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "turbo run format",
    "test": "turbo run test",
    "type": "turbo run type"
  },
  "devDependencies": {
    "prettier": "^2.8.1",
    "turbo": "^1.6.3"
  },
  "engines": {
    "node": "18.12.1"
  },
  "dependencies": {
    "typescript": "4.7.4"
  }
}
